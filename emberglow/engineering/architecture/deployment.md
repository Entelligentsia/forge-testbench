<!-- AUTO-GENERATED by /forge:init — confidence: 70% -->
# Deployment

## Environments

| Environment | Status | Notes |
|-------------|--------|-------|
| Development | Active | Local Go development |
| CI/CD | Not configured [?] | No CI pipeline exists |
| Production | Not deployed | Local-only tool |

## Build Targets

| Platform | Status |
|----------|--------|
| Linux (amd64) | Supported via Go cross-compilation |
| macOS | Supported via Go cross-compilation |
| Windows | Supported via Go cross-compilation |

## Build Artifact

```
go build -o bin/emberglow ./cmd/emberglow
```

Binary is self-contained (no external runtime dependencies).

## Installation

[?] Installation method not yet defined. Options include:
- `go install`
- Homebrew tap
- Direct binary download

## Distribution

- No package manager integration [?]
- No container image [?]
- No cloud deployment targets

## Development Workflow

```bash
go run ./cmd/emberglow <command>   # Run without build
go build -o bin/emberglow ...       # Build binary
go test ./...                       # Run tests
go vet ./...                        # Lint
```