<!-- AUTO-GENERATED by /forge:init — confidence: 90% -->
# Stack Checklist

Review items for the emberglow project based on discovered stack and testing setup.

## Go 1.22

- [ ] Go toolchain installed (1.22+)
- [ ] `go.mod` declares correct module path
- [ ] Dependencies resolved with `go mod tidy`

## YAML Parsing (gopkg.in/yaml.v3)

- [ ] Recipe files are valid YAML
- [ ] `Parse()` function handles YAML unmarshal errors
- [ ] All required fields are validated

## TUI (charmbracelet/bubbletea)

- [ ] TUI entry point defined [?]
- [ ] Bubbletea model implemented [?]
- [ ] Lipgloss styles defined [?]

## Testing

- [ ] No test files exist yet
- [ ] `go test ./...` passes (currently would find no tests)
- [ ] Unit tests for `dsl.Parse()` [?]
- [ ] Unit tests for `Engine.Run()` [?]

## Linting

- [ ] `go vet ./...` passes
- [ ] Novet issues in CI [?]

## Build

- [ ] `go build -o bin/emberglow ./cmd/emberglow` succeeds
- [ ] Binary is self-contained
- [ ] Cross-platform builds work [?]

## Code Quality

- [ ] Standard Go formatting (`gofmt`)
- [ ] Package-level doc comments on all packages
- [ ] Exported types have doc comments
- [ ] All functions return `error` explicitly
- [ ] No panics on user-supplied input

## Known Issues to Address

- [ ] `cmd/emberglow/main.go` does not exist — implement CLI entry point
- [ ] `Engine.Run()` blocks on `select{}` — implement real event loop
- [ ] `Unless *Trigger` parsed but not evaluated
- [ ] `Delay` field on `Action` not implemented
- [ ] Device drivers not yet implemented