[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hello"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["click"]

[project.scripts]
hello = "hello:main"
