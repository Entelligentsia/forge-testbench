<!-- AUTO-GENERATED by /forge:init — confidence: 92% -->

# Entity Model — cartographer

## Domain Overview

cartographer is a terminal knowledge-graph CLI tool (`carto`) that lets users build and navigate a personal graph of ideas, concepts, and topics entirely offline. The graph is persisted as a single JSON file at `~/.cartographer/graph.json`. There is no server, no database engine, and no network dependency. All data structures are flat and serialised directly to/from JSON.

The domain centres on three entities: **Graph** (the root container), **Node** (a concept or idea), and **Edge** (a directed relationship between two nodes).

## Entity Definitions

### Graph

The top-level container. One Graph exists per user installation. It is loaded in full on every command invocation and written back in full on any mutation.

| Field   | Type     | Description                                      |
|---------|----------|--------------------------------------------------|
| `nodes` | `Node[]` | All nodes in the graph (unordered array)         |
| `edges` | `Edge[]` | All directed relationships between nodes         |

Persistence path: `~/.cartographer/graph.json`

---

### Node

The primary domain entity. Represents a single idea, concept, or topic.

| Field       | Type       | Required | Description                                                           |
|-------------|------------|----------|-----------------------------------------------------------------------|
| `id`        | `string`   | yes      | UUID generated by `randomUUID()` at creation time; stable identifier |
| `title`     | `string`   | yes      | Human-readable display name; used as the primary lookup key (case-sensitive) |
| `body`      | `string`   | yes      | Free-text content or description of the concept                       |
| `tags`      | `string[]` | yes      | Zero or more categorisation tags                                      |
| `createdAt` | `string`   | yes      | ISO 8601 timestamp set at creation; never mutated after that          |
| `updatedAt` | `string`   | yes      | ISO 8601 timestamp updated on every mutation                          |

Notes:
- `title` is the user-facing lookup key in all CLI commands (e.g. `carto link "Source" "Target"`).
- `id` (UUID) is used internally by Edge references and is never exposed directly to the user in the current CLI.
- Duplicate titles are not currently prevented by the implementation [?].

---

### Edge

A directed relationship between two Nodes.

| Field    | Type     | Required | Description                                                      |
|----------|----------|----------|------------------------------------------------------------------|
| `from`   | `string` | yes      | `Node.id` of the source node                                     |
| `to`     | `string` | yes      | `Node.id` of the target node                                     |
| `label`  | `string` | no       | Optional human-readable description of the relationship type     |
| `weight` | `number` | yes      | Numeric edge weight; always `1` in the current implementation    |

Notes:
- Directionality is encoded: `from → to`. Reverse traversal requires a separate edge.
- `label` is optional; many edges will have no label.
- `weight` exists in the schema for future use (e.g. ranking, shortest-path); its value is always `1` today.
- Self-loops (where `from === to`) are not explicitly prevented [?].

## Relationships

```
Graph
 ├── nodes: Node[]    (one Graph owns zero or more Nodes)
 └── edges: Edge[]    (one Graph owns zero or more Edges)

Edge.from ──→ Node.id  (each Edge references exactly one source Node by id)
Edge.to   ──→ Node.id  (each Edge references exactly one target Node by id)
```

- The relationship between Graph and Node/Edge is **composition**: nodes and edges exist only within a Graph and are destroyed with it.
- The relationship between Edge and Node is a **directed reference** (not a foreign-key constraint): the implementation does not currently enforce referential integrity, so dangling edge references are possible if a Node is deleted without cleaning up its edges [?].
- Node-to-Node relationships are **many-to-many** via Edge; a single Node may appear as `from` or `to` in any number of edges.

## Invariants

The following invariants are intended by the design. Items marked [?] are inferred from code and may not be actively enforced at runtime.

1. **Unique node id**: Every Node has a UUID assigned at creation. UUIDs are unique by construction (`randomUUID()`).
2. **Title as lookup key**: All CLI commands that reference a node do so by `title` (case-sensitive string match). Titles should be unique within a Graph; the current implementation does not enforce this [?].
3. **Edge references valid nodes**: `Edge.from` and `Edge.to` must each correspond to a `Node.id` present in `Graph.nodes`. Referential integrity is not enforced programmatically in the current implementation [?].
4. **Immutable createdAt**: `Node.createdAt` is set once at creation and never updated.
5. **Edge weight is 1**: `Edge.weight` is always set to `1`; no command currently accepts a custom weight.
6. **Offline-only persistence**: The graph file is the sole storage medium. No remote sync, no transactions, no locking — concurrent writes from multiple processes would corrupt the file [?].
7. **Graph is loaded and saved in full**: There is no partial update; every mutation reads the entire file, applies the change in memory, and writes the entire file back.
