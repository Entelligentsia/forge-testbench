<!-- AUTO-GENERATED by /forge:init — confidence: 92% -->

# Deployment

## Distribution Model

cartographer is distributed as an npm package. The `bin` field in `package.json` maps the `carto` command to `./dist/cli.js`:

```json
"bin": { "carto": "./dist/cli.js" }
```

The package is intended for local installation on a developer's machine. It is not published to a public npm registry [?]; users install directly from source. The compiled output in `dist/` is generated by `tsc` at build time and is not checked into version control.

User data is stored locally at `~/.cartographer/graph.json`. No remote storage or network access is required at runtime.

## Install Flow

1. Clone the repository.
2. Run `npm install` to install all dependencies.
3. Run `npm run build` to compile TypeScript sources (`tsc` → `dist/`).
4. Run `npm link` (or `npm install -g`) to make the `carto` command available system-wide.

During development, the build step can be skipped by using the dev runner:

```bash
npm run dev -- <command>
```

This invokes `tsx src/cli.ts` directly, bypassing the `dist/` output entirely.

## Environments

There is a single environment: the user's local machine.

| Property | Value |
|---|---|
| Runtime | Node.js 20+ |
| Module system | ESM (`"type": "module"`) |
| Data directory | `~/.cartographer/` |
| Data file | `~/.cartographer/graph.json` |
| Required env vars | None (uses `HOME` implicitly via Node's `os.homedir()` [?]) |

There are no staging, preview, or production environments. All usage is local and offline-only by design.

## CI/CD Pipeline

There is no CI/CD pipeline. No `.github/workflows`, `.gitlab-ci.yml`, or equivalent configuration file is present in the repository.

Verification steps (tests, lint, build) must be run manually:

```bash
npm test       # vitest run
npm run lint   # eslint src
npm run build  # tsc
```
