<!-- AUTO-GENERATED by /forge:init — confidence: 88% -->
# CART Stack & Testing Checklist

Quick-reference review checklist for the `cartographer` / `carto` CLI project.
Items marked `[?]` are inferred from discovery and should be verified against source.

---

## Code Style Checklist

- [ ] All TypeScript source files use `.ts` extension; all ESM imports reference `.js` extensions (e.g. `import { ... } from "../types.js"`)
- [ ] `"type": "module"` is set in `package.json` — never mix `require()` / CommonJS patterns
- [ ] Use `const` and arrow functions; avoid `class` definitions unless adding Ink TUI components
- [ ] Success output uses `console.log` with `chalk.green` or `chalk.cyan`; error output uses `console.error` with `chalk.red`
- [ ] No database or network imports — project is offline-only by design (`lowdb` is a declared dependency but currently unused)
- [ ] Node lookup uses case-sensitive `title` matching — do not change this behaviour without a deliberate decision
- [ ] `types.ts` interfaces (`Node`, `Edge`, `Graph`) stay flat — no class hierarchies
- [ ] `graph.ts` exports pure functions only — no singleton state, no module-level side effects
- [ ] Only `cli.ts` performs I/O side effects (`console.log`, `chalk` output, `process.exit`)
- [ ] `tsconfig.json` `strict: true` is retained — do not disable strict checks
- [ ] `moduleResolution` is set to `"bundler"` in `tsconfig.json` — keep consistent with ESM + tsx dev runner [?]

---

## Testing Checklist

- [ ] Test framework is `vitest` v1 — do not introduce Jest or Mocha
- [ ] Run tests with `npm test` (`vitest run` — non-watch mode)
- [ ] Test files live in `src/__tests__/` and `src/store/` (co-located with the module under test)
- [ ] `fs` module is mocked via `vi.mock("fs", async (importOriginal) => { ... })` — keep the `importOriginal` async pattern
- [ ] `crypto` module (`randomUUID`) is mocked via `vi.mock("crypto", () => ({ randomUUID: vi.fn(...) }))` for deterministic IDs
- [ ] `save()` regression guard: test confirms `mkdirSync` is called **before** `writeFileSync` (CART-B01)
- [ ] `addNode()` tests verify `title`, `body`, `tags`, and `id` fields on the returned `Node`
- [ ] `link()` tests verify that a missing source or target node throws `"Node not found"`
- [ ] `load()` tests verify an empty `{ nodes: [], edges: [] }` graph is returned when the file does not exist
- [ ] Use `beforeEach(() => vi.clearAllMocks())` in every `describe` block that uses mocks
- [ ] No coverage tooling configured — add `vitest --coverage` to `package.json` before enforcing thresholds [?]
- [ ] No eslint config file detected in the project root — verify `npm run lint` resolves config before relying on it [?]

---

## Build Checklist

- [ ] Build with `npm run build` (invokes `tsc`) — output goes to `dist/`
- [ ] Development runner is `npm run dev -- <args>` (invokes `tsx src/cli.ts`) — no build step required
- [ ] `bin` entry in `package.json` points to `./dist/cli.js` — ensure `dist/` is populated before packaging or running as installed binary
- [ ] `rootDir` is `src/` and `outDir` is `dist/` in `tsconfig.json` — do not add source files outside `src/`
- [ ] Target is `ES2022` — do not downgrade; Node 20+ supports all ES2022 features
- [ ] `skipLibCheck: true` is set — acceptable for this project; do not add third-party `@types/*` that conflict with existing deps [?]
- [ ] `dist/` should be excluded from version control (verify `.gitignore` includes `dist/`) [?]

---

## Architecture Guardrails

- [ ] Data is persisted to `~/.cartographer/graph.json` — do not change this path without a migration path for existing users
- [ ] `save()` must call `mkdirSync(dir, { recursive: true })` **before** `writeFileSync` — regression for CART-B01 (previously broken with `await import("fs")` inside a sync function)
- [ ] `DATA_PATH` is derived from `process.env.HOME ?? "~"` — keep this pattern; do not hardcode home paths
- [ ] `addNode` generates IDs via `randomUUID()` from Node's built-in `crypto` module — do not swap for a third-party UUID library
- [ ] `Graph`, `Node`, `Edge` interfaces are the single source of truth for the data model — all persistence and CLI code must derive from `types.ts`
- [ ] `lowdb` is listed as a dependency but is not used in the codebase — remove or integrate intentionally; do not leave unused deps silently [?]
- [ ] `enquirer` is listed as a dependency but no usage detected in `src/` — verify it is needed or remove [?]
- [ ] `ink` and `react` are runtime dependencies reserved for a future TUI — do not remove; do not import them until TUI work begins
- [ ] `exportMarkdown()` iterates all nodes and edges in memory — acceptable for small graphs; flag for review if node count grows large
- [ ] Edge `weight` is hardcoded to `1` in `link()` — intentional default; expose as a CLI option only if explicitly required
